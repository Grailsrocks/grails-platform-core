h1. p:organization

h2. Purpose

Renders the name of the company or organisation that set in Config.

The Config value is set in "plugin.platformCore.organization.name"

h2. Example

{code:xml}
<p:organization/>
<p:organization encodeAs="JavaScript"/>
{code}

h2. Attributes

{table}
*Name* | *Purpose* 
encodeAs | Optional - which codec to use when encoding the output. Default is HTML.
{table}

