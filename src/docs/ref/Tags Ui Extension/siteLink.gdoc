h1. p:siteLink

h2. Purpose

Renders an absolute link to site, using the site name as the text.

See [siteName|tagsUiExtension] for details of how to specify the site name.

h2. Example

{code:xml}
<p:siteLink/>
{code}

h2. Attributes

All attributes are passed through to g:link - the body of the g:link is the result of p:siteName.

