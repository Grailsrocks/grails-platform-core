h1. s:createLoginLink

h2. Purpose

Returns a URL that will take the user to a login screen.

This gets the URL from the security provider that is installed.

To render a button that will take the user to this page, see [loginButton|securityTags].

h2. Example

{docx:xml}
<s:createLoginLink/>
{docx}

h2. Attributes

None.